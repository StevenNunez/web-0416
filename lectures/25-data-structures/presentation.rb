require 'slide_hero'

presentation '25-data-structures' do
  slide "Presentations made easy!" do
    point "Make presentations at the speed of thought"
    point "Make ideas pop", animation: "grow"
    list do
      point "All kinds of awesome here"
    end
  end
end
=begin
  Recursion
    Base Case
    Counting
    Each
    Map
    Reduce/inject
  Linked lists
    #insert(aNode) # Adds node at the end    
    #find_by_value(value) # returns nodes    
    #insert_at(position, aNode) # places node after position    
    #delete_at(position) # deletes node at position    
    #set_head(aNode) # sets new head 
=end
